function Game({rows, columns, bombs}){

    return {
        uncoverCell({row, colum}){
            console.log(`Row: ${row}, colum: ${column}`)
        }
        placeFlag({row, colum}){
            console.log(`Row: ${row}, colum: ${column}`)
        }
    }
}

export default Game
